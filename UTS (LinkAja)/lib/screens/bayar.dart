import 'package:flutter/material.dart';

class Bayar extends StatelessWidget {
  const Bayar({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Halaman Bayar'),
      ),
    );
  }
}
